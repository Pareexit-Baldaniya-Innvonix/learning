// Task 4:

class Car {
    constructor(make, model, year) {
        this.make = make;
        this.model = model;
        this.year = year;
    }
}

const car = new Car("BMW", "R8", 2025);
console.log(car.make);
console.log(car.model);
console.log(car.year);